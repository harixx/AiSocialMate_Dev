# SocialMonitor AI Environment Configuration Template
# Copy this file to .env and fill in your API keys

# ===========================================
# REQUIRED API KEYS
# ===========================================

# OpenAI API Key - Get from https://platform.openai.com/api-keys
# Required for AI reply generation
OPENAI_API_KEY=sk-your-openai-api-key-here

# Serper API Key - Get from https://serper.dev/
# Required for social media search functionality
SERPER_API_KEY=your-serper-api-key-here

# ===========================================
# OPTIONAL CONFIGURATION
# ===========================================

# Database URL (optional - uses in-memory storage if not provided)
# Example: postgresql://user:password@localhost:5432/socialmonitor
DATABASE_URL=

# Server Configuration
NODE_ENV=development
PORT=5000
HOST=0.0.0.0

# ===========================================
# DEPLOYMENT PLATFORMS
# ===========================================

# Railway deployment:
# 1. Add OPENAI_API_KEY in Railway dashboard > Variables
# 2. Add SERPER_API_KEY in Railway dashboard > Variables

# Render deployment:
# 1. Add OPENAI_API_KEY in Render dashboard > Environment
# 2. Add SERPER_API_KEY in Render dashboard > Environment

# Heroku deployment:
# 1. heroku config:set OPENAI_API_KEY=sk-your-key
# 2. heroku config:set SERPER_API_KEY=your-key

# Docker deployment:
# 1. Use docker run -e OPENAI_API_KEY=sk-your-key -e SERPER_API_KEY=your-key
# 2. Or add to docker-compose.yml environment section